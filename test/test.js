const expect    = require("chai").expect;
const inputData = require("../app.js");
const cities = require("../app.js");

describe("Pull Data From Text File", function() {
  describe("Insert Data into an Array", function() {
    it("Outputs an Array of Data", function() {
      expect(inputData).to.equal!=null;
    });
  });
});

describe("Sorts Cites into its own array", function() {
  if("Outpusts an array of cities", function() {
    expect(cities).to.equal!=null;
  });
});